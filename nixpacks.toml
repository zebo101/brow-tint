[phases.install]
cmds = [
  "pnpm install --frozen-lockfile --ignore-scripts",
  "node scripts/setup-heroui-pro.mjs",
  "pnpm exec fumadocs-mdx"
]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm start"

