# 导航与首页分享图补充修复

## 改动

- 8 个语言的顶级导航在 Blog 与 Pricing 之间新增 `/filter`；英文为 `Try On / Filter`。
- 删除共享 metadata 中把网站 URL 填入 `twitter.site` 的代码。没有配置未经确认的 X 品牌账号。
- 所有语言首页使用 `/imgs/og/browlens-home-20260914.jpg`，同时输出 OG 图片宽高 1200×630。Twitter 使用同一图片。博客仍使用各自的文章配图。
- 分享图来自 2026-09-14 的实际生产首页截图，裁去顶部账号区域和下方历史记录，保留标题、示例人像、上传区与眉型网格。JPEG 文件 54,721 字节。

## 验证

- TypeScript `tsc --noEmit --incremental`：通过。
- 现有 `blog-seo`、`seo-metadata`、`seo-paths`：10 项测试通过。
- 本次修改文件的 Prettier 与 `git diff --check`：通过。
- 独立只读代码审查：未发现问题；实际 JPEG 已打开检查并确认尺寸。
- 本地开发服务首页首次编译长时间未完成，停止本次临时服务，未将 HTTP 检查或浏览器布局验收记为通过。

部署验收运行 `node scripts/verify-social-seo.mjs https://browlens.com`：检查 8 个语言首页实际 HTML 的导航顺序、OG/Twitter 图片、图片宽高、无效字段删除、博客配图保留及静态图片 HTTP 200 / JPEG 内容。另检查 1024px / 1280px 桌面与手机菜单；如果导航拥挤，缩短本地化标签或调整该断点的导航间距。此脚本的 HTTP 验证尚待部署任务执行。

## 下一阶段：eyebrow mapping

重点页面为 `https://browlens.com/blog/eyebrow-mapping-guide`。首页与 `/filter` 已有直接内容链接；博客列表也可进入，文章并非孤岛。

目录结果返回后，逐条记录来源页面、实际目标 URL、是否公开可访问、链接是否存在、`rel` 属性和主题相关性。允许文章提交且与眉毛或美妆内容相关的来源，优先使用文章 URL；仅接收产品主页的目录保持真实产品介绍。不能仅凭提交成功或 DR 数值认定产生了排名价值。

用户后续明确：现有这一批目录提交的是首页 URL，保持现状；文章直链另从评测、教程、榜单或相关提及寻找。本轮不扩展外链工作，没有提交新外链或修改既有目录条目。目录链接仍可能通过首页的文章内链间接帮助文章。后续排名观察应使用文章的 GSC 页面数据与目标查询数据，先确认收录，再记录曝光、点击和平均排名。
